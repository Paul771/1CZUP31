
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("АвтоТест") Тогда // Возврат при получении формы для анализа.
		Возврат;
	КонецЕсли;
	
	ЗаполнитьЗначенияСвойств(ЭтотОбъект, Параметры, "Сотрудник,Основание,СерияДокумента,НомерДокумента,КемВыданДокумент");	
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ОК(Команда)
	
	Оповестить("ВведеныРеквизитыДокументаПодтверждающегоКвалификацию", ВведенныеДанные(), ЭтаФорма.ВладелецФормы);
	Закрыть();
	
КонецПроцедуры

&НаКлиенте
Процедура Отмена(Команда)
	
	Закрыть();
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаКлиенте
Функция ВведенныеДанные()
	
	ВведенныеДанные = Новый Структура;
	 
	 ВведенныеДанные.Вставить("Сотрудник", ЭтотОбъект.Сотрудник);
	 ВведенныеДанные.Вставить("Основание", ЭтотОбъект.Основание);
	 ВведенныеДанные.Вставить("СерияДокумента", ЭтотОбъект.СерияДокумента);
	 ВведенныеДанные.Вставить("НомерДокумента", ЭтотОбъект.НомерДокумента);
	 ВведенныеДанные.Вставить("КемВыданДокумент", ЭтотОбъект.КемВыданДокумент);
	 
	 Возврат ВведенныеДанные;
	 
КонецФункции

#КонецОбласти
