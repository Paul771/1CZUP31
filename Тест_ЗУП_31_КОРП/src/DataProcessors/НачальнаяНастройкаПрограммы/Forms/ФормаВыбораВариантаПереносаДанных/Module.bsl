#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("АвтоТест") Тогда
		Возврат;
	КонецЕсли;
	
	ПереноситьНачисленияПредыдущейПрограммы = Параметры.ПереноситьНачисленияПредыдущейПрограммы;
	
	ПолныйПеренос = ?(ПереноситьНачисленияПредыдущейПрограммы,1,0);
	УпрощенныйПеренос = ?(ПереноситьНачисленияПредыдущейПрограммы,0,1);
	
КонецПроцедуры

&НаКлиенте
Процедура ПередЗакрытием(Отказ, ЗавершениеРаботы, ТекстПредупреждения, СтандартнаяОбработка)
	
	Если ЗавершениеРаботы Тогда
		Возврат;
	КонецЕсли;
	
	Если РазрешитьЗакрытие Тогда
		Возврат;
	КонецЕсли;		
		
	СтандартнаяОбработка = Ложь;
	РазрешитьЗакрытие = Истина;
	
	РезультатВыбора = Новый Структура;
	РезультатВыбора.Вставить("ПереноситьНачисленияПредыдущейПрограммы", (ПолныйПеренос = 1));
	Закрыть(РезультатВыбора);
	
КонецПроцедуры

&НаКлиенте
Процедура УпрощенныйПереносПриИзменении(Элемент)
	
	Если УпрощенныйПеренос = 1 Тогда
		ПолныйПеренос = 0;
	КонецЕсли;	
	
КонецПроцедуры

&НаКлиенте
Процедура ПолныйПереносПриИзменении(Элемент)
	
	Если ПолныйПеренос = 1 Тогда
		УпрощенныйПеренос = 0;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти